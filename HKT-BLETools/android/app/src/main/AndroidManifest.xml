<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 禁止手机休眠 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- SD卡读写权限 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <category android:name="android.intent.category.OPENABLE" />
            <data android:mimeType="application/octet-stream" />
        </intent>
    </queries>
    <uses-permission
        android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- neverForLocation：扫描仅用于发现设备广播名，不用于定位——
         缺此标记时系统同时要求定位运行时授权，卸载重装后（定位未授权）扫描恒零结果（真机实证） -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- 适配 Android 12 的附近设备权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.NEARBY_DEVICES" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_tool"
        android:localeConfig="@xml/locales_config"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:requestLegacyExternalStorage="true"
        android:enableOnBackInvokedCallback="true"
        android:roundIcon="@mipmap/ic_tool"
        android:supportsRtl="true"
        android:theme="@style/Theme.BLETools"
        android:hardwareAccelerated="true"
        tools:targetApi="tiramisu">

        <!-- 应用内语言持久化由 BaseApp 自管（SharedPreferences），不启用 appcompat 的
             autoStoreLocales：该机制与 ComposeActivity（非 AppCompatActivity）组合会在
             进程重启后的 onCreate 触发 SavedStateRegistry 双重恢复 → 必崩（真机 FD 实证） -->

        <!-- v3.21 旧 UI（V6.0.0b1 起退居二线：类保留可回退，入口经 adb 全限定名可达） -->
        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <activity
            android:name=".QrCodeActivity"
            android:clearTaskOnLaunch="true"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="stateAlwaysHidden"/>

        <activity android:name=".DeviceActivity" android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".DebugActivity"/>

        <!-- v2 Compose 宿主：V6.0.0b1+1 起为唯一 launcher（M8 裁决） -->
        <activity
            android:name=".ComposeActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BLETools">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
